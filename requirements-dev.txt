# Test-only dependencies. The runtime image does not need these; install them when
# running the suite:  .venv_tutor/bin/pip install -r requirements-dev.txt && pytest
-r requirements.txt

pytest>=8.0
